package dev.pyrulen.permutations;

import java.util.Set;

public class StreamingPermutationProvider implements PermutationProvider {
    @Override
    public Set<char[]> getPermutations(char[] source) {
        return Set.of();
    }
}
